spring:
  sql:
    init:
      mode: always
      schema-locations: schema.sql
      data-locations: data.sql
  shell:
    interactive:
      enabled: false
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format-sql: true
    show-sql: true